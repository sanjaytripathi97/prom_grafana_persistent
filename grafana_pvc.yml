apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pvc
  labels:
    env: graph-visuals
spec:
  resources:
    requests:
      storage: 5Gi
  accessModes:
  - ReadWriteMany